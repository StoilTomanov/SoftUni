function townInfo(town, population, area) {
    let line = `Town ${town} has population of ${population} and area ${area} square km.`
    console.log(line);
}
townInfo('Sofia', 1286383, 492);

// tested in Judge = 100/100